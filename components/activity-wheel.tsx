"use client";
import { useEffect,useMemo,useRef,useState } from "react";import { RotateCcw,Shuffle } from "lucide-react";import { useRouter } from "next/navigation";
import { randomEligibleActivities } from "@/data/activities";import { activityFilterOptions,defaultFilters,filterActivities,type ActivityFilters } from "@/lib/activity-utils";import { localizeActivity } from "@/lib/activity-localization";import { FILTER_STORAGE_KEY,RANDOM_HISTORY_KEY,RECENT_RANDOM_KEY } from "@/lib/navigation";import { nextRandomHistory,nextWheelRotation,pickWheelItemIndex,selectedIndexFromRotation,selectWheelItems,splitWheelTitle,wheelFontSize } from "@/lib/random-wheel";import type { Activity } from "@/types/activity";import { useLanguage } from "@/hooks/use-language";
const colors=["#6c4cf1","#8b6cf6","#4f63d8","#a454d2","#5579c6","#7852b9","#397f8e","#b55d78"];
const readJson=<T,>(key:string,fallback:T):T=>{try{return JSON.parse(localStorage.getItem(key)||"") as T}catch{return fallback}};
type SpinResult={activity:Activity;id:string;slug:string;title:string;index:number;rotation:number};
export function ActivityWheel(){const {language,t}=useLanguage();const router=useRouter();const redirectRef=useRef<ReturnType<typeof setTimeout>|null>(null);const spinFallbackRef=useRef<ReturnType<typeof setTimeout>|null>(null);const completedRef=useRef(false);const [filters,setFilters]=useState<ActivityFilters>(defaultFilters);const [items,setItems]=useState<Activity[]>(()=>randomEligibleActivities);const [history,setHistory]=useState<string[]>([]);const [rotation,setRotation]=useState(0);const [spinning,setSpinning]=useState(false);const [selectedResult,setSelectedResult]=useState<SpinResult|null>(null);
 const candidates=useMemo(()=>filterActivities(randomEligibleActivities,filters),[filters]);const background=useMemo(()=>items.length?items.map((_,index)=>`${colors[index%colors.length]} ${index*360/items.length}deg ${(index+1)*360/items.length}deg`).join(","):"#eceaf4 0deg 360deg",[items]);const hasFilters=Object.keys(filters).some(key=>filters[key as keyof ActivityFilters]!==defaultFilters[key as keyof ActivityFilters]);
 useEffect(()=>{const timer=setTimeout(()=>{const saved={...defaultFilters,...readJson<Partial<ActivityFilters>>(FILTER_STORAGE_KEY,{})};const savedHistory=readJson<string[]>(RANDOM_HISTORY_KEY,[]);const next=filterActivities(randomEligibleActivities,saved);setFilters(saved);setHistory(savedHistory);setItems(next)},0);return()=>clearTimeout(timer)},[]);
 useEffect(()=>()=>cancelTimers(),[]);
 function cancelTimers(){if(redirectRef.current)clearTimeout(redirectRef.current);if(spinFallbackRef.current)clearTimeout(spinFallbackRef.current);redirectRef.current=null;spinFallbackRef.current=null}
 function clearSelection(){cancelTimers();completedRef.current=false;setSelectedResult(null);setSpinning(false);setRotation(0)}
 function update<K extends keyof ActivityFilters>(key:K,value:ActivityFilters[K]){const next={...filters,[key]:value};clearSelection();setFilters(next);localStorage.setItem(FILTER_STORAGE_KEY,JSON.stringify(next));setItems(filterActivities(randomEligibleActivities,next))}
 function resetFilters(){clearSelection();setFilters(defaultFilters);localStorage.setItem(FILTER_STORAGE_KEY,JSON.stringify(defaultFilters));setItems(randomEligibleActivities)}
 function spin(){if(spinning||selectedResult||!items.length)return;cancelTimers();completedRef.current=false;const selectedIndex=pickWheelItemIndex(items,history);if(selectedIndex<0)return;const selectedActivity=items[selectedIndex];const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const finalRotation=nextWheelRotation(rotation,selectedIndex,items.length,reduced?1:6);const result:SpinResult={activity:selectedActivity,id:selectedActivity.id,slug:selectedActivity.slug||selectedActivity.id,title:localizeActivity(selectedActivity,language).title,index:selectedIndex,rotation:finalRotation};setSelectedResult(result);setSpinning(true);if(process.env.NODE_ENV!=="production")console.debug("Random activity selected",{wheelItems:items.map(item=>({id:item.id,slug:item.slug||item.id,title:localizeActivity(item,language).title})),selectedIndex,id:result.id,slug:result.slug,title:result.title,rotation:finalRotation});requestAnimationFrame(()=>requestAnimationFrame(()=>setRotation(finalRotation)));spinFallbackRef.current=setTimeout(()=>completeSpin(result,items),reduced?350:3800)}
 function completeSpin(intended:SpinResult,wheelItems:Activity[]){if(completedRef.current)return;completedRef.current=true;if(spinFallbackRef.current)clearTimeout(spinFallbackRef.current);spinFallbackRef.current=null;const visualIndex=selectedIndexFromRotation(intended.rotation,wheelItems.length,0);const visualActivity=wheelItems[visualIndex];if(!visualActivity)return;const result:SpinResult={activity:visualActivity,id:visualActivity.id,slug:visualActivity.slug||visualActivity.id,title:localizeActivity(visualActivity,language).title,index:visualIndex,rotation:intended.rotation};if(process.env.NODE_ENV!=="production"){console.debug("Wheel result verified",{intendedIndex:intended.index,visualIndex,intendedSlug:intended.slug,visualSlug:result.slug,finalRotation:intended.rotation});if(intended.index!==visualIndex||intended.id!==result.id)console.error("Wheel result mismatch",{intendedIndex:intended.index,visualIndex,intendedActivity:intended.activity,visualActivity,finalRotation:intended.rotation})}setSelectedResult(result);setSpinning(false);const next=nextRandomHistory(history,result.id);setHistory(next);localStorage.setItem(RANDOM_HISTORY_KEY,JSON.stringify(next));localStorage.setItem(RECENT_RANDOM_KEY,result.id);const targetSlug=result.slug;redirectRef.current=setTimeout(()=>{if(process.env.NODE_ENV!=="production")console.debug("Navigating to selected activity",{id:result.id,slug:targetSlug});router.push(`/activities/${targetSlug}`)},800)}
 function shuffle(){clearSelection();setItems(selectWheelItems(candidates,candidates.length,Math.random,items.map(item=>item.id)))}
 const filterLabels={level:t("filter.level"),duration:t("activity.duration"),groupSize:t("filter.group"),category:t("filter.category")};const status=spinning?t("randomActivity.spinning"):selectedResult?`${selectedResult.title} ${t("randomActivity.selected")}`:t("randomActivity.description");
 return <section className="wheel-section"><div className="wheel-filter-panel"><header><div><b>{hasFilters?t("common.filter"):t("nav.activities")}</b><p>{candidates.length} {t("randomActivity.available")}</p></div>{hasFilters&&<button disabled={spinning||Boolean(selectedResult)} onClick={resetFilters}><RotateCcw/>{t("randomActivity.reset")}</button>}</header><div>{(["level","duration","groupSize","category"] as const).map(key=><label key={key}>{filterLabels[key]}<select value={filters[key]} disabled={spinning||Boolean(selectedResult)} onChange={event=>update(key,event.target.value as never)}>{activityFilterOptions[key].map(value=><option key={value}>{value}</option>)}</select></label>)}</div></div>
  <div className="wheel-status" aria-live="polite">{status}{selectedResult&&!spinning&&<small>{t("randomActivity.opening")}</small>}</div>{!candidates.length?<div className="wheel-empty"><h2>{t("randomActivity.noMatches")}</h2><button className="button button-primary" onClick={resetFilters}>{t("randomActivity.reset")}</button></div>:<><div className="wheel-wrap"><div className="wheel-pointer"/><div className="activity-wheel" data-visible-count={items.length} onTransitionEnd={event=>{if(event.propertyName==="transform"&&selectedResult)completeSpin(selectedResult,items)}} style={{"--wheel-font-size":`${wheelFontSize(items.length)}px`,background:`conic-gradient(${background})`,transform:`rotate(${rotation}deg)`,transitionDuration:spinning?"3.6s":"0s"} as React.CSSProperties}>{items.map((item,index)=>{const localized=localizeActivity(item,language);const lines=splitWheelTitle(localized.shortTitle);const angle=index*360/items.length+180/items.length;const flipped=angle>90&&angle<270;return <span key={item.id} className={flipped?"is-flipped":""} data-wheel-index={index} title={localized.title} style={{transform:`rotate(${angle}deg)`}}><i>{lines.map(line=><em key={line}>{line}</em>)}</i></span>})}</div><button className="spin-button" onClick={spin} disabled={spinning||Boolean(selectedResult)}>{spinning?"…":t("randomActivity.spin")}</button></div>{candidates.length>1&&<button className="shuffle-wheel" disabled={spinning||Boolean(selectedResult)} onClick={shuffle}><Shuffle/>{t("randomActivity.shuffle")}</button>}</>}</section>}
