"use client";import { Check,Globe2 } from "lucide-react";import { useEffect,useRef,useState } from "react";import { useLanguage } from "@/hooks/use-language";import { languageCodes,languageNames,supportedLanguages } from "@/types/language";
export function LanguageSelector(){const {language,setLanguage,t}=useLanguage();const [open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const close=(event:MouseEvent)=>{if(!ref.current?.contains(event.target as Node))setOpen(false)};const escape=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.addEventListener("mousedown",close);document.addEventListener("keydown",escape);return()=>{document.removeEventListener("mousedown",close);document.removeEventListener("keydown",escape)}},[]);return <div className="language-selector" ref={ref}><button aria-label={t("nav.language")} aria-haspopup="listbox" aria-expanded={open} onClick={()=>setOpen(!open)}><Globe2/><span>{languageCodes[language]}</span></button>{open&&<div className="language-menu" role="listbox" aria-label={t("nav.language")}>{supportedLanguages.map(item=><button role="option" aria-selected={language===item} onClick={()=>{setLanguage(item);setOpen(false)}} key={item}><span>{languageNames[item]}</span><small>{languageCodes[item]}</small>{language===item&&<Check/>}</button>)}</div>}</div>}
