"use client";

import { Plus,X } from "lucide-react";
import { useState } from "react";
export function PracticeExpressionForm({onSave,disabled}:{onSave:(value:{expression:string;koreanMeaning:string;example:string})=>boolean;disabled:boolean}){const [open,setOpen]=useState(false);const [expression,setExpression]=useState("");const [meaning,setMeaning]=useState("");const [example,setExample]=useState("");const [error,setError]=useState("");function submit(e:React.FormEvent){e.preventDefault();if(!expression.trim()){setError("영어 표현을 입력해 주세요.");return;}if(!onSave({expression,koreanMeaning:meaning,example})){setError("중복 표현이거나 최대 5개가 등록되어 있습니다.");return;}setExpression("");setMeaning("");setExample("");setError("");setOpen(false);}if(!open)return <button className="button button-secondary" disabled={disabled} onClick={()=>setOpen(true)}><Plus/>직접 입력</button>;return <form className="practice-form" onSubmit={submit}><div className="practice-form-head"><b>새 실전 표현</b><button type="button" onClick={()=>setOpen(false)} aria-label="입력 닫기"><X/></button></div><label>영어 표현<input value={expression} onChange={(e)=>setExpression(e.target.value)} placeholder="I’ll keep that in mind."/></label><label>한국어 뜻 <small>선택</small><input value={meaning} onChange={(e)=>setMeaning(e.target.value)} placeholder="기억해 둘게요."/></label><label>간단한 예문 <small>선택</small><input value={example} onChange={(e)=>setExample(e.target.value)} placeholder="You should book early. I’ll keep that in mind."/></label>{error&&<p role="alert">{error}</p>}<button className="button button-primary" type="submit">저장</button></form>}
