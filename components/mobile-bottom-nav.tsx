"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { BookOpenText, Dices, Home, LayoutGrid, UserRound } from "lucide-react";
import { useLanguage } from "@/hooks/use-language";
export function MobileBottomNav(){const path=usePathname();const {t}=useLanguage();const items=[{href:"/",label:t("common.home"),icon:Home},{href:"/activities",label:t("nav.activities"),icon:LayoutGrid},{href:"/random",label:t("common.random"),icon:Dices},{href:"/expressions",label:t("nav.expressions"),icon:BookOpenText},{href:"/my-study",label:t("nav.myStudy"),icon:UserRound}];return <nav className="mobile-bottom-nav" aria-label="Primary navigation">{items.map(({href,label,icon:Icon})=>{const base=href.split("?")[0];const active=base==="/"?path==="/":path.startsWith(base);return <Link href={href} data-tutorial={href==="/my-study"?"my-study":undefined} className={active?"is-active":""} key={href}><Icon/><span>{label}</span></Link>})}</nav>}
