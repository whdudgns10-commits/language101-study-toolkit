"use client";
import Link from "next/link"; import { ArrowLeft, Home } from "lucide-react";import { useLanguage } from "@/hooks/use-language";
export function PageHeader({title,description,titleKey,descriptionKey}:{title:string;description:string;titleKey?:string;descriptionKey?:string}){const {t}=useLanguage();return <header className="page-header section-shell"><Link href="/" aria-label={t("common.back")}><ArrowLeft/></Link><div><h1>{titleKey?t(titleKey):title}</h1><p>{descriptionKey?t(descriptionKey):description}</p></div><Link href="/" aria-label={t("common.home")}><Home/></Link></header>}
