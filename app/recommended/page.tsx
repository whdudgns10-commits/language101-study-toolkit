import { PageHeader } from "@/components/page-header"; import { QuickStartModes } from "@/components/quick-start-modes"; import { ActivityCard } from "@/components/activity-card"; import { activities } from "@/data/activities";
const defaults=["30-second-speaking","20-questions","guessing-words","useful-expressions"];
export const metadata={title:"오늘의 추천 | Language101 Study Toolkit"}; export default function Page(){return <main className="subpage"><PageHeader title="Recommended for Today" description="어떤 활동을 고를지 고민될 때 여기서 시작하세요."/><section className="section-shell recommended-default"><h2>오늘 바로 하기 좋은 활동</h2><div className="activity-grid">{defaults.map(id=>activities.find(x=>x.id===id)).filter(Boolean).map(x=>x&&<ActivityCard activity={x} key={x.id}/>)}</div></section><QuickStartModes/></main>}
