import type { Metadata } from "next";
import { SpyfallAdmin } from "@/components/2026-07-26-spyfall-admin";

export const metadata: Metadata = {
  title: "Find the Spy Admin | Language101",
};

export default function SpyfallAdminPage() {
  return <SpyfallAdmin/>;
}
